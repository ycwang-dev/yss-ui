import type { YssSheetLocale } from '../../types';

const sheet: YssSheetLocale = {
  newWorkbook: 'New Workbook',
  loading: 'Loading sheet...',
  loadFailed: 'Failed to load sheet',
};

export default sheet;
