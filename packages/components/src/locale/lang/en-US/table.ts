import type { YssTableLocale } from '../../types';

const table: YssTableLocale = {
  actionTitle: 'Actions',
  more: 'More',
  confirmTitle: 'Are you sure to {action} this item?',
  total: 'Total {total} items',
};

export default table;
